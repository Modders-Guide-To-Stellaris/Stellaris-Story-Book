namespace = ssp_deficit

event = {
    id = ssp_deficit.0
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        random = {
            chance = 90
            set_global_flag = BritishMuseumVanquished
        }
    }
}


country_event = {
    id = ssp_deficit.1
    title = ssp_deficit.1.name
    desc = ssp_deficit.1.desc
    event_picture_background = GFX_spaceport_background #todo: replace me
    is_triggered_only = yes

    option = {
        name = DEWIT
        set_country_flag = broken_caches
        add_resource = { minor_artifacts = 5000 }
    }
    option = {
        name = RECONSIDER

    }
}

# Negative Income
country_event = {
    id = ssp_deficit.10
    title = ssp_deficit.10.name
    desc = ssp_deficit.10.desc
    show_sound = event_negative_income_advisor
    is_triggered_only = yes

    is_advisor_event = yes

    trigger = {
        is_ai = no
        running_balance < -1
        NOR = {
            has_country_flag = advisor_losing_artifact
            is_tutorial_level = 0
        }
    }

    immediate = {
        set_country_flag = advisor_losing_artifact
    }

    option = {
        name = OK
    }
}

country_event = {
    id = ssp_deficit.20
    title = ssp_relicseized.name
    desc = ssp_deficit.20.desc
    event_picture_background = GFX_mega_art_installation_background
    is_triggered_only = yes

    immediate = {
        hidden_effect = {
            country_event = { id = ssp_deficit.20b }
        }
    }

    option = {
        name = OHNO
        set_country_flag = British_Seized_Assets_High
        allow = {
            NOT = {
                OR = {
                    is_gestalt = yes
                    has_country_flag = DeniedBritish
                }
            }
        }
    }
    option = {
        name = BRITISHDENIED
        add_resource = { minor_artifacts = 1000 }
        add_modifier = { modifier = ssp_raided_british days = 180 }
        country_event = { id = ssp_deficit.20c days = 180 }
        set_country_flag = DeniedBritish
    }
}

country_event = {
    #The British Museum seizes a relic
    id = ssp_deficit.20a1
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = {
            limit = {
                NOT = {
                    has_global_flag = BritishMuseumVanquished
                }
            }
            country_event = { id = ssp_deficit.20a2 }
        }
    }

}


country_event = {
    #The British Museum seizes a relic
    id = ssp_deficit.20a2
    title = ssp_relicseized.name
    desc = ssp_deficit.20a.desc
    event_picture_background = GFX_evt_relic_world
    is_triggered_only = yes

    #Soncio if you stumble upon this, I don't know of a better way to get the list of relics, so this is the "how to do it phase". If you ever figure out
    #How to get the relics an empire owns, lmk cause that would be super useful for this xD
    immediate = {
        locked_random_list = {
            1 = { if = { limit = { has_relic = r_ancient_sword } remove_relic = r_ancient_sword } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_celestial_tear } remove_relic = r_celestial_tear } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_cetanas_heart } remove_relic = r_cetanas_heart } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_continuum } remove_relic = r_continuum } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_cryo_core } remove_relic = r_cryo_core } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_eternal_throne } remove_relic = r_eternal_throne } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_ever_spinning_top } remove_relic = r_ever_spinning_top } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_galatron } remove_relic = r_galatron } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_galaxy } remove_relic = r_galaxy } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_infinity_root } remove_relic = r_infinity_root } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_khans_throne } remove_relic = r_khans_throne } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_daedalus_seal } remove_relic = r_daedalus_seal } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_dragon_trophy } remove_relic = r_dragon_trophy } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_odryskan_crystal } remove_relic = r_odryskan_crystal } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_omnicodex } remove_relic = r_omnicodex } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_plasmic_core } remove_relic = r_plasmic_core } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_pox_sample } remove_relic = r_pox_sample } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_prethoryn_queen } remove_relic = r_prethoryn_queen } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_reality_perforator } remove_relic = r_reality_perforator } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_rubricator } remove_relic = r_rubricator } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_severed_head } remove_relic = r_severed_head } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_surveyor } remove_relic = r_surveyor } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_the_defragmentor } remove_relic = r_the_defragmentor } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_the_last_baol } remove_relic = r_the_last_baol } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_the_rulerchip } remove_relic = r_the_rulerchip } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_contingency_core } remove_relic = r_contingency_core } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_time_crystal } remove_relic = r_time_crystal } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_toxic_god } remove_relic = r_toxic_god } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_unbidden_warlock } remove_relic = r_unbidden_warlock } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_vacuum_flower } remove_relic = r_vacuum_flower } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_war_forge } remove_relic = r_war_forge } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_worm_scales } remove_relic = r_worm_scales } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = r_zro_crystal } remove_relic = r_zro_crystal } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = ssp_r_animarum } remove_relic = ssp_r_animarum } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = ssp_r_drive_core } remove_relic = ssp_r_drive_core } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = ssp_r_khyran_bacterium } remove_relic = ssp_r_khyran_bacterium } else = { country_event = { id = ssp_deficit.20d }}}
            1 = { if = { limit = { has_relic = ssp_r_xenomorph_queen } remove_relic = ssp_r_xenomorph_queen } else = { country_event = { id = ssp_deficit.20d }}}
        }
        set_country_flag = British_Seized_Assets_Low
    }

    option = {
        name = WAIT
    }
}


country_event = {
    id = ssp_deficit.20c
    title = ssp_relicseized.name
    desc = ssp_deficit.20.desc
    event_picture_background = GFX_mega_art_installation_background
    is_triggered_only = yes


}

country_event = {
    #Fails to seize
    id = ssp_deficit.20d
    title = ssp_relicseizedfail.name
    desc = ssp_deficit.20a.desc
    event_picture_background = GFX_evt_relic_world
    is_triggered_only = yes

    immediate = {
        random_owned_leader = {
            limit = {
                NOT = {
                    is_councilor = yes
                }
            }
            kill_leader = {}
        }
    }
}

country_event = {
    id = ssp_deficit.21
    title = ssp_vanquished_british
    desc = ssp_vanquished_british.desc
    event_picture_background = GFX_mega_art_installation_background
    is_triggered_only = yes

    immediate = {
        if = {
            limit = {
                has_country_flag = British_Seized_Assets_Low
            }
            country_event = { id = ssp_deficit.22 }
        }
        else_if = {
            limit = {
                has_country_flag = British_Seized_Assets_High
            }
            country_event = { id = ssp_deficit.22 }
            country_event = { id = ssp_deficit.22 }
            random = {
                chance = 5
                country_event = { id = ssp_deficit.22 }
            }
        }
    }
}

country_event = {
    id = ssp_deficit.22
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        locked_random_list = {
            1 = { if = { limit = { NOT = { has_relic = r_ancient_sword }} add_relic = r_ancient_sword }}
            1 = { if = { limit = { NOT = { has_relic = r_celestial_tear }} add_relic = r_celestial_tear }}
            1 = { if = { limit = { NOT = { has_relic = r_continuum }} add_relic = r_continuum }}
            1 = { if = { limit = { NOT = { has_relic = r_ever_spinning_top }} add_relic = r_ever_spinning_top }}
            1 = { if = { limit = { NOT = { has_relic = r_galatron }} add_relic = r_galatron }}
            1 = { if = { limit = { NOT = { has_relic = r_galaxy }} add_relic = r_galaxy }}
            1 = { if = { limit = { NOT = { has_relic = r_infinity_root }} add_relic = r_infinity_root }}
            1 = { if = { limit = { NOT = { has_relic = r_khans_throne }} add_relic = r_khans_throne }}
            1 = { if = { limit = { NOT = { has_relic = r_cryo_core }} add_relic = r_cryo_core }}
            1 = { if = { limit = { NOT = { has_relic = r_khans_throne }} add_relic = r_khans_throne }}
            1 = { if = { limit = { NOT = { has_relic = r_daedalus_seal }} add_relic = r_daedalus_seal }}
            1 = { if = { limit = { NOT = { has_relic = r_dragon_trophy }} add_relic = r_dragon_trophy }}
            1 = { if = { limit = { NOT = { has_relic = r_odryskan_crystal }} add_relic = r_odryskan_crystal }}
            1 = { if = { limit = { NOT = { has_relic = r_omnicodex }} add_relic = r_omnicodex }}
            1 = { if = { limit = { NOT = { has_relic = r_plasmic_core }} add_relic = r_plasmic_core }}
            1 = { if = { limit = { NOT = { has_relic = r_pox_sample }} add_relic = r_pox_sample }}
            1 = { if = { limit = { NOT = { has_relic = r_prethoryn_queen }} add_relic = r_prethoryn_queen }}
            1 = { if = { limit = { NOT = { has_relic = r_reality_perforator }} add_relic = r_reality_perforator }}
            1 = { if = { limit = { NOT = { has_relic = r_rubricator }} add_relic = r_rubricator }}
            1 = { if = { limit = { NOT = { has_relic = r_severed_head }} add_relic = r_severed_head }}
            1 = { if = { limit = { NOT = { has_relic = r_surveyor }} add_relic = r_surveyor }}
            1 = { if = { limit = { NOT = { has_relic = r_the_defragmentor }} add_relic = r_the_defragmentor }}
            1 = { if = { limit = { NOT = { has_relic = r_the_last_baol }} add_relic = r_the_last_baol }}
            1 = { if = { limit = { NOT = { has_relic = r_the_rulerchip }} add_relic = r_the_rulerchip }}
            1 = { if = { limit = { NOT = { has_relic = r_contingency_core }} add_relic = r_contingency_core }}
            1 = { if = { limit = { NOT = { has_relic = r_time_crystal }} add_relic = r_time_crystal }}
            1 = { if = { limit = { AND = { has_relic = r_toxic_god NOT = { any_country = { any_situation = { is_situation_type = situation_toxic_knights}}}}} add_relic = r_toxic_god }}
            1 = { if = { limit = { AND = { NOT = { has_relic = r_vacuum_flower } NOT = { any_country = { any_situation = { is_situation_type = situation_kaleidoscope}}}}} add_relic = r_vacuum_flower }}
            1 = { if = { limit = { NOT = { has_relic = r_war_forge }} add_relic = r_war_forge }}
            1 = { if = { limit = { NOT = { has_relic = r_worm_scales }} add_relic = r_worm_scales }}
            1 = { if = { limit = { NOT = { has_relic = r_zro_crystal }} add_relic = r_zro_crystal }}
            1 = { if = { limit = { AND = { NOT = { has_relic = ssp_r_animarum } NOT = { any_country = { any_situation = { is_situation_type = situation_grimoire_decipher}}}}} add_relic = ssp_r_animarum }}
            1 = { if = { limit = { NOT = { has_relic = ssp_r_drive_core }} add_relic = ssp_r_drive_core }}
            1 = { if = { limit = { NOT = { has_relic = ssp_r_khyran_bacterium }} add_relic = ssp_r_khyran_bacterium }}
            1 = { if = { limit = { NOT = { has_relic = ssp_r_xenomorph_queen }} add_relic = ssp_r_xenomorph_queen }}
        }
    }
}

country_event = {
    #The British Museum seizes all relics
    id = ssp_deficit.200
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_ancient_sword } remove_relic = r_ancient_sword }
        country_event = { id = ssp_deficit.201 days = 1}
    }
}

country_event = {
    id = ssp_deficit.201
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_celestial_tear } remove_relic = r_celestial_tear }
        country_event = { id = ssp_deficit.202 days = 1}
    }
}

country_event = {
    id = ssp_deficit.202
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_cetanas_heart } remove_relic = r_cetanas_heart }
        country_event = { id = ssp_deficit.203 days = 1}
    }
}

country_event = {
    id = ssp_deficit.203
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_continuum } remove_relic = r_continuum }
        country_event = { id = ssp_deficit.204 days = 1}
    }
}

country_event = {
    id = ssp_deficit.204
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_cryo_core } remove_relic = r_cryo_core }
        country_event = { id = ssp_deficit.205 days = 1}
    }
}

country_event = {
    id = ssp_deficit.205
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_eternal_throne } remove_relic = r_eternal_throne }
        country_event = { id = ssp_deficit.206 days = 1}
    }
}

country_event = {
    id = ssp_deficit.206
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_ever_spinning_top } remove_relic = r_ever_spinning_top }
        country_event = { id = ssp_deficit.207 days = 1}
    }
}

country_event = {
    id = ssp_deficit.207
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_galatron } remove_relic = r_galatron }
        country_event = { id = ssp_deficit.208 days = 1}
    }
}

country_event = {
    id = ssp_deficit.208
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_galaxy } remove_relic = r_galaxy }
        country_event = { id = ssp_deficit.209 days = 1}
    }
}

country_event = {
    id = ssp_deficit.209
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_infinity_root } remove_relic = r_infinity_root }
        country_event = { id = ssp_deficit.210 days = 1}
    }
}

country_event = {
    id = ssp_deficit.210
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_khans_throne } remove_relic = r_khans_throne }
        country_event = { id = ssp_deficit.211 days = 1}
    }
}

country_event = {
    id = ssp_deficit.211
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_daedalus_seal } remove_relic = r_daedalus_seal }
        country_event = { id = ssp_deficit.212 days = 1}
    }
}

country_event = {
    id = ssp_deficit.212
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_dragon_trophy } remove_relic = r_dragon_trophy }
        country_event = { id = ssp_deficit.213 days = 1}
    }
}

country_event = {
    id = ssp_deficit.213
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_odryskan_crystal } remove_relic = r_odryskan_crystal }
        country_event = { id = ssp_deficit.214 days = 1}
    }
}

country_event = {
    id = ssp_deficit.214
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_omnicodex } remove_relic = r_omnicodex }
        country_event = { id = ssp_deficit.215 days = 1}
    }
}

country_event = {
    id = ssp_deficit.215
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_plasmic_core } remove_relic = r_plasmic_core }
        country_event = { id = ssp_deficit.216 days = 1}
    }
}

country_event = {
    id = ssp_deficit.216
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_pox_sample } remove_relic = r_pox_sample }
        country_event = { id = ssp_deficit.217 days = 1}
    }
}

country_event = {
    id = ssp_deficit.217
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_prethoryn_queen } remove_relic = r_prethoryn_queen }
        country_event = { id = ssp_deficit.218 days = 1}
    }
}

country_event = {
    id = ssp_deficit.218
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_reality_perforator } remove_relic = r_reality_perforator }
        country_event = { id = ssp_deficit.219 days = 1}
    }
}

country_event = {
    id = ssp_deficit.219
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_rubricator } remove_relic = r_rubricator }
        country_event = { id = ssp_deficit.220 days = 1}
    }
}

country_event = {
    id = ssp_deficit.220
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_severed_head } remove_relic = r_severed_head }
        country_event = { id = ssp_deficit.221 days = 1}
    }
}

country_event = {
    id = ssp_deficit.221
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_surveyor } remove_relic = r_surveyor }
        country_event = { id = ssp_deficit.222 days = 1}
    }
}

country_event = {
    id = ssp_deficit.222
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_the_defragmentor } remove_relic = r_the_defragmentor }
        country_event = { id = ssp_deficit.223 days = 1}
    }
}

country_event = {
    id = ssp_deficit.223
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_the_last_baol } remove_relic = r_the_last_baol }
        country_event = { id = ssp_deficit.224 days = 1}
    }
}

country_event = {
    id = ssp_deficit.224
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_the_rulerchip } remove_relic = r_the_rulerchip }
        country_event = { id = ssp_deficit.225 days = 1}
    }
}

country_event = {
    id = ssp_deficit.225
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_contingency_core } remove_relic = r_contingency_core }
        country_event = { id = ssp_deficit.226 days = 1}
    }
}

country_event = {
    id = ssp_deficit.226
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_time_crystal } remove_relic = r_time_crystal }
        country_event = { id = ssp_deficit.227 days = 1}
    }
}

country_event = {
    id = ssp_deficit.227
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_toxic_god } remove_relic = r_toxic_god }
        country_event = { id = ssp_deficit.228 days = 1}
    }
}

country_event = {
    id = ssp_deficit.228
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_unbidden_warlock } remove_relic = r_unbidden_warlock }
        country_event = { id = ssp_deficit.229 days = 1}
    }
}

country_event = {
    id = ssp_deficit.229
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_vacuum_flower } remove_relic = r_vacuum_flower }
        country_event = { id = ssp_deficit.230 days = 1}
    }
}

country_event = {
    id = ssp_deficit.230
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_war_forge } remove_relic = r_war_forge }
        country_event = { id = ssp_deficit.231 days = 1}
    }
}

country_event = {
    id = ssp_deficit.231
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_worm_scales } remove_relic = r_worm_scales }
        country_event = { id = ssp_deficit.232 days = 1}
    }
}

country_event = {
    id = ssp_deficit.232
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = r_zro_crystal } remove_relic = r_zro_crystal }
        country_event = { id = ssp_deficit.233 days = 1}
    }
}

country_event = {
    id = ssp_deficit.233
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = ssp_r_animarum } remove_relic = ssp_r_animarum }
        country_event = { id = ssp_deficit.234 days = 1}
    }
}

country_event = {
    id = ssp_deficit.234
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = ssp_r_drive_core } remove_relic = ssp_r_drive_core }
        country_event = { id = ssp_deficit.235 days = 1}
    }
}

country_event = {
    id = ssp_deficit.235
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = ssp_r_khyran_bacterium } remove_relic = ssp_r_khyran_bacterium }
        country_event = { id = ssp_deficit.236 days = 1}
    }
}

country_event = {
    id = ssp_deficit.236
    hide_window = yes
    is_triggered_only = yes

    immediate = {
        if = { limit = { has_relic = ssp_r_xenomorph_queen } remove_relic = ssp_r_xenomorph_queen }
    }
}
